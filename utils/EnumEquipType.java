

package ws.billy.CookieGadgets.utils;

public enum EnumEquipType
{
    REPLACE("REPLACE", 0), 
    WARN("WARN", 1), 
    DROP("DROP", 2);
    
    private EnumEquipType(final String name, final int ordinal) {
    }
}
